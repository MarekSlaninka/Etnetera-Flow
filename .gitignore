# macOS
.DS_Store

# Xcode
build/
DerivedData/
*.xcuserstate
*.xcworkspace/xcuserdata/
xcuserdata/
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Swift Package Manager
.build/
.swiftpm/

# Firebase CLI
.firebase/

# Scratch
tmp/

# Local agent tooling
AGENTS.md
.codex/

# Firebase app config, supplied separately.
# GoogleService-Info.plist.example at the repo root documents its structure —
# it lives outside the app folder so the synchronized group does not bundle it.
Etnetera-Flow/GoogleService-Info.plist
